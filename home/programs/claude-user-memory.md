# Global rules

## Commit style

- Always use [Conventional Commits](https://www.conventionalcommits.org/) for git commit
  messages: a `type(optional scope): description` subject line (e.g. `feat:`, `fix:`,
  `docs:`, `chore:`, `refactor:`, `test:`, `build:`, `ci:`), with optional body and
  footer. Use `!` or a `BREAKING CHANGE:` footer for breaking changes.

## Comment and slop standards

Apply these to every surface you author: code comments, docs, commit messages,
and PR or MR descriptions. The comment standard imports the general slop
taxonomy, so the line below loads both.

@~/.claude/standards/comment-rules.md
