# PLACEHOLDER. Recreate as a sops-encrypted file before bootstrapping a host:
#   sops secrets/tailscale.yaml
#
#   tailscale-authkey: tskey-auth-...
#
# Generate at https://login.tailscale.com/admin/settings/keys as reusable and
# pre-approved. Keys last 90 days and are read only during enrollment, so an
# expired one blocks new hosts and leaves enrolled hosts alone.
tailscale-authkey: PLACEHOLDER
